-- kham_pg--0.3.0--0.6.0.sql
--
-- Upgrade from 0.3.0 to 0.6.0.
-- No SQL schema changes in this release — version bumped to align with
-- kham-core 0.6.0.  The Rust shared library is replaced automatically
-- by `make install`; no DDL is required.
--
-- This file exists so that ALTER EXTENSION kham_pg UPDATE succeeds.

-- intentionally empty
