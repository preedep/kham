# kham-tnc NER Validator dependencies
fastapi>=0.110
uvicorn[standard]>=0.27
# pythainlp manages its own model downloads — no HuggingFace login required
# NER engine "thainer" uses WangchanBERTa internally via pythainlp's corpus manager
pythainlp>=5.0
# torch: macOS PyPI wheel is always CPU (no CUDA on macOS).
# Linux GPU: add --index-url https://download.pytorch.org/whl/cu121
torch>=2.2
transformers>=4.38
sentencepiece>=0.1.99
